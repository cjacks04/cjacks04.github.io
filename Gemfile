source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme", group: :jekyll_plugins

# Required when building older GitHub Pages/Jekyll dependency stacks on Ruby 3.x.
# Ruby no longer loads REXML as part of the default runtime in this environment,
# but kramdown/Jekyll still require it.
gem "rexml"
